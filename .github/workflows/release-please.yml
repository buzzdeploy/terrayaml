name: Release Please Merge
on:
  push:
    branches:
      - main
      - remote-release

permissions:
  contents: write
  pull-requests: write

jobs:
  tag-and-release:
    uses: buzzdeploy/tf-module-template/.github/workflows/release-terraform.yml@sast